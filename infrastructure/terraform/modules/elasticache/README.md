# ElastiCache Module

Creates a Redis 7 replication group with encryption at rest and in transit.

## Key Inputs

- `node_type`, `num_cache_nodes`, `auth_token`
- `subnet_ids`, `security_group_ids`

## Key Outputs

- `redis_endpoint`, `redis_port`
