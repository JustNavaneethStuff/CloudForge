# Route53 Module

Creates a hosted zone and ACM certificate for HTTPS (feature-flagged).

## Key Inputs

- `domain_name`, `api_subdomain`

## Key Outputs

- `hosted_zone_id`, `certificate_arn`, `certificate_status`
